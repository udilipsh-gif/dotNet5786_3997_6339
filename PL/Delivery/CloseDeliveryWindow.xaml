<Window x:Class="PL.CloseDeliveryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL"
        Title="סיום משלוח" 
        Height="250" Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="CloseDeliveryWindow_Loaded"
        Closed="CloseDeliveryWindow_Closed">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="סיום משלוח" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
            <TextBlock Text="אנא בחר את סיבת סיום המשלוח:" FontSize="14" Foreground="{StaticResource SubTextColor}" Margin="0,5,0,0"/>
        </StackPanel>

        <ComboBox Grid.Row="1" 
                  x:Name="EndDeliveryComboBox"
                  ItemsSource="{Binding EndDeliveryOptions}"
                  SelectedItem="{Binding SelectedEndDelivery, Mode=TwoWay}"
                  VerticalAlignment="Top"
                  Height="40">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" Padding="5"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
            <Button Content="אישור" 
                    Width="100" 
                    Click="OkButton_Click"
                    IsDefault="True"/>

            <Button Content="ביטול" 
                    Width="100" 
                    Click="CancelButton_Click"
                    IsCancel="True"
                    Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>